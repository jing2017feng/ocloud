server:
  port: 7030
spring:
  application:
    name: ocloud-service
  main:
    allow-bean-definition-overriding: true
  cloud:
    config:
      enabled: true
      profile: dev
      uri: http://localhost:8771/